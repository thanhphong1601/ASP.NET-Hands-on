chay dotnet ef database update để tạo/up database
tạo class DataExtensions: https://www.youtube.com/watch?v=YbRe4iIVYJk&t=3626s 2h



Đã làm:
- Tích hợp db: SQLite, install package SQLite, Design for db miragtion; tạo DbContext.cs, chỉnh lại field của các model, tạo class DataExtensions.MiragteDb for data migration khi mở app
- Fix lại các Controllers, Interfaces và Services de lưu xuống database
- Thêm các exception vào GlobalExceptionHandler
- Apply health check, config
- Chỉnh sửa AuthController, tạo AuthService, implement function check username, password then issue jwt
- Implement Refit: Fetch product list từ api https://dummyjson.com/products, config, tạo interface ProductsFetchingApiByUrl

To do:
- Tạo repository
- ASP.NET Identity Provider
- Mô tả api cho swagger
- Dùng virtual properties
- Dùng include thay cho Join
- Dùng câu IQueryable ExecuteDelete/Update de chạy nhanh duoi db
- Apply paginaton
- Migrate Sql server
- Không dung IActionResult trả về trong controller, nên làm sao de trả về object, có thể là json gồm object type + data, CẦN 1 OBJECT DE MAP, DÙNG TẤT CẢ MỌI NƠI = API RESPONSE giống trong Refit
-Tạo mediator de call từ api den application


NOTE: use AsNoTracking() sẽ k cache khi lấy data lên 